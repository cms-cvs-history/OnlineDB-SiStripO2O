process o2o = {

    service = MessageLogger {
	untracked vstring destinations = { "cout" }
	untracked PSet cout = { untracked string threshold = "INFO" }
	untracked vstring debugModules   = { "*" }
    }
    
    source = EmptyIOVSource {
	string timetype = "runnumber"
        untracked uint32 firstRun = 1
	untracked uint32 lastRun = 1
        uint32 interval = 1
    }
    untracked PSet maxEvents = {untracked int32 input = 1}
  

    service = SiStripConfigDb { 
# 	untracked    bool UsingDb         = true
# 	untracked  string ConfDb          = "insert_ConfigDbFull"
#         untracked  string Partition       = "insert_ConfigDbPartition"
#         untracked  uint32 MajorVersion    = insert_ConfigDbMajorVersion
#        untracked  uint32 MinorVersion    = insert_ConfigDbMinorVersion

# 	untracked    bool UsingDb         = true
# 	untracked  string ConfDb          = "cms_tracker_tif2/stabletif2@cms_tec_lyon"
#         untracked  string Partition       = "TIBTOBPLUS_07032007"
#         untracked  uint32 MajorVersion    = 3
#         untracked  uint32 MinorVersion    = 0


	untracked    bool UsingDb         = true
	untracked  string ConfDb          = "cms_tracker_tif2/stabletif2@cms_tec_lyon"
        untracked  string Partition       = "TIBTOBPLUS_07032007"
	untracked uint32 MajorVersion = 0 //@@ cabling
	untracked uint32 MinorVersion = 0 //@@ cabling
	untracked uint32 FedMajorVersion = 35
	untracked uint32 FedMinorVersion = 0
	untracked uint32 FecMajorVersion = 1
	untracked uint32 FecMinorVersion = 0

# 	untracked    bool UsingDb         = true
# 	untracked  string ConfDb          = "cms_tracker_tif3/stabletif3@cms_sstracker"
#         untracked  string Partition       = "TIBTOBPLUS_07032007"
# 	untracked uint32 MajorVersion = 1 //@@ cabling
# 	untracked uint32 MinorVersion = 0 //@@ cabling
# 	untracked uint32 FedMajorVersion = 1
# 	untracked uint32 FedMinorVersion = 0
# 	untracked uint32 FecMajorVersion = 1
# 	untracked uint32 FecMinorVersion = 0

    }


    es_source FedCablingFromConfigDb = SiStripFedCablingBuilderFromDb {
	untracked string CablingSource = "UNDEFINED"
    }
    es_module sistripconn = SiStripConnectivity {}

    es_source PedFromConfigDb = SiStripPedestalsBuilderFromDb {}

    es_source NoiseFromConfigDb = SiStripNoiseBuilderFromDb {}

    
    service = PoolDBOutputService{
	string connect = "sqlite_file:dbfile.db"    
	string timetype = "runnumber"    
	untracked string BlobStreamerName="TBufferBlobStreamingService"
	PSet DBParameters = {
	    untracked string authenticationPath="/afs/cern.ch/cms/DB/conddb"
	    untracked uint32 messagelevel = 4 
	}
	 
	VPSet toPut={ 
	    { string record = "SiStripPedestalsRcd"  string tag = "insert_tagPN_p"} ,
	    { string record = "SiStripNoisesRcd"     string tag = "insert_tagPN_n"} ,
	    { string record = "SiStripFedCablingRcd" string tag = "insert_tagCab"} 
	}
    }    
    
    
    module siStripO2O= newSiStripO2O {
	untracked bool   printDebug = true
	untracked bool   UsingDb    = true			 
    }	
    
   path p = { siStripO2O }
    
    module print = AsciiOutputModule {}
    endpath ep = { print }
}
